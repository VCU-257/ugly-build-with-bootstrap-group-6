export default function Cart() {
  // Need to add cart page and add routing to get there on click
    // Cart icon should show number of items in cart
  // Cart should be usable whether signed in or not like amazon
  return <div>Cart</div>;
}